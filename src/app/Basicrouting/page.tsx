import React from 'react'

export default function page() {
  return (
    <div>
      <h1>Next JS uses file system based routing</h1>
      <p>when creating a directory in app folder then always use page.js file name</p>
      <h2>this is basic routing or stage first routing</h2>
    </div>
  );
}
